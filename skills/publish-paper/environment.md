# Compatibility Note

Environment setup guidance has moved to:

```text
../prepare-staging/environment.md
```

Use it during `/prepare-staging` when the publication has runnable code, notebooks, figure/table scripts, paper build tooling, or external software requirements.

